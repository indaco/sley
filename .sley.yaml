# sley configuration file
# Documentation: https://github.com/indaco/sley

# Enabled plugins:
#   - commit-parser
#   - tag-manager
#   - changelog-generator

path: internal/version/.version

plugins:
  # Analyzes conventional commits to suggest version bumps
  commit-parser: true
  # Manages git tags - use `sley tag create` for manual tagging
  tag-manager:
    enabled: true
  # Generates changelogs from git commits
  changelog-generator:
    enabled: true
    repository:
      auto-detect: true
    use-default-icons: true
    contributors:
      enabled: true
      show-name: false
extensions:
  - name: version-sync
    path: .sley-extensions/version-sync
    enabled: true
