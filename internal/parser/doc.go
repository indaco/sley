// Package parser provides a unified interface for reading version information
// from various file formats including JSON, YAML, TOML, raw text, and regex patterns.
// It is used by both the dependency-check plugin and the discovery service.
package parser
