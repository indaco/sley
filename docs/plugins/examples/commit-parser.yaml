# Commit Parser Plugin Example
# See: docs/plugins/COMMIT_PARSER.md

path: .version

plugins:
  # Enabled by default - configure only to disable
  commit-parser: true

  # To disable:
  # commit-parser: false
